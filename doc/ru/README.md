# Vue3 Dropzone

Гибкий компонент dropzone для Vue 3 с объединёнными режимами предпросмотмотра и редактирования. Отлично подходит для загрузки файлов, галерей изображений и интерфейсов управления товарами.
    <a href="https://vue3-dropzone-docs.vercel.app/"><strong>Демо</strong></a>

## 🚀 Начало работы

### Установка

```bash
npm install @astro-masters/vue3-dropzone-ts
# или
yarn add @astro-masters/vue3-dropzone-ts
```

### Базовое использование

```js
// В вашем main.js
import { createApp } from 'vue'
import Vue3Dropzone from '@astro-masters/vue3-dropzone-ts'
import "@astro-masters/vue3-dropzone-ts/style.css" // Не забудьте подключить стили!

const app = createApp(App)
app.component('Vue3Dropzone', Vue3Dropzone)

// ИЛИ внутри вашего компонента (локальная регистрация)
import Vue3Dropzone from '@astro-masters/vue3-dropzone-ts'
import "@astro-masters/vue3-dropzone-ts/style.css"
```

### Минимальный пример

```vue
<template>
  <Vue3Dropzone v-model="files" />
</template>

<script setup>
import { ref } from 'vue'

const files = ref([])
</script>
```

### Полный пример

```vue
<template>
  <Vue3Dropzone 
    v-model="files" 
    multiple
    width="100%" 
    height="400px" 
    imgWidth="45%" 
    imgHeight="300px"
    :maxFileSize="10"
    :maxFiles="2"
    :state="state"
  />
</template>

<script setup>
import { ref } from 'vue'

const files = ref([])
const state = ref('indeterminate') // Может быть: 'indeterminate', 'success' или 'error'
</script>
```

## TypeScript

Пакет поставляется с типами TypeScript и реэкспортирует их из главного entry.

### Пример с типизированным v-model

```vue
<template>
  <Vue3Dropzone
    v-model="files"
    v-model:previews="previews"
    mode="edit"
    multiple
  />
</template>

<script setup lang="ts">
import { ref } from 'vue'
import Vue3Dropzone, { type DropzoneFileItem } from '@astro-masters/vue3-dropzone-ts'

const files = ref<DropzoneFileItem[]>([])
const previews = ref<string[]>([])
</script>
```

### Справочник типов

| Тип | Описание |
|---|---|
| `DropzoneMode` | `'drop' | 'preview' | 'edit'` |
| `DropzoneState` | `'error' | 'success' | 'indeterminate'` |
| `DropzoneStatus` | `'pending' | 'uploading' | 'success' | 'error'` |
| `DropzonePreviewPosition` | `'inside' | 'outside'` |
| `DropzoneSelectFileStrategy` | `'replace' | 'merge'` |
| `DropzoneItemType` | `'file' | 'url'` |
| `DropzoneBaseItem` | Общие поля элемента предпросмотра |
| `DropzoneFileItem` | Элемент с реальным `File` (`type: 'file'`) |
| `DropzoneUrlItem` | Элемент для существующего URL (`type: 'url'`) |
| `DropzoneItem` | Union: `DropzoneFileItem | DropzoneUrlItem` |
| `DropzoneErrorType` | Тип ошибки валидации / запросов |
| `DropzoneErrorEvent` | Payload события `error`: `{ type, files }` |
| `DropzoneUploadRequestEvent` | Payload события `upload-request` (внешний обработчик загрузки) |
| `DropzoneRemoveRequestEvent` | Payload события `remove-request` (внешний обработчик удаления) |

### Поддерживаемые версии

- Vue.js 3.x
- Современные браузеры с поддержкой ES6

## Серверная загрузка/удаление: внешние обработчики (events)

Когда включён `serverSide`, компонент может загружать/удалять файлы двумя способами:

- Через ваши обработчики событий `upload-request` / `remove-request`
- Через встроенный `XMLHttpRequest` (fallback), если слушателей событий нет

Так вы можете использовать `axios`, `fetch`, свою авторизацию и любые контракты API, при этом компонент продолжит:

- Обновлять `fileItem.status`, `fileItem.message`, `fileItem.progress`
- Эмитить `fileUploaded` при успехе
- Эмитить `error` с `type: 'upload-error' | 'delete-error'` при ошибках

### Пример загрузки (Axios + прогресс)

```vue
<template>
  <Vue3Dropzone
    v-model="files"
    :server-side="true"
    upload-endpoint="https://api.example.com/upload"
    server-file-id-key="file_id"
    :headers="{ Authorization: `Bearer ${token}` }"
    @upload-request="onUploadRequest"
  />
</template>

<script setup lang="ts">
import { ref } from 'vue'
import axios from 'axios'
import Vue3Dropzone, { type DropzoneFileItem, type DropzoneUploadRequestEvent } from '@astro-masters/vue3-dropzone-ts'

const token = '...'
const files = ref<DropzoneFileItem[]>([])

const onUploadRequest = async (e: DropzoneUploadRequestEvent) => {
  try {
    const response = await axios.post(e.endpoint, e.formData, {
      headers: e.headers,
      onUploadProgress: (pe) => {
        if (!pe.total) return
        e.progress((pe.loaded / pe.total) * 100)
      },
    })
    e.success(response?.data)
  } catch (err: any) {
    e.error(err?.message ?? 'Загрузка не удалась', err)
  }
}
</script>
```

### Пример удаления (Axios)

```vue
<template>
  <Vue3Dropzone
    v-model="files"
    :server-side="true"
    delete-endpoint="https://api.example.com/files"
    :headers="{ Authorization: `Bearer ${token}` }"
    @remove-request="onRemoveRequest"
  />
</template>

<script setup lang="ts">
import axios from 'axios'
import type { DropzoneRemoveRequestEvent } from '@astro-masters/vue3-dropzone-ts'

const token = '...'

const onRemoveRequest = async (e: DropzoneRemoveRequestEvent) => {
  try {
    await axios.delete(e.endpoint, { headers: e.headers })
    e.success()
  } catch (err: any) {
    e.error(err?.message ?? 'Удаление не удалось', err)
  }
}
</script>
```

## 🎯 Ключевые возможности

### **Единая модель данных**

- **Несколько источников данных**: объединяет объекты `File` (новые загрузки) и строки URL (уже существующие файлы)
- **Умное определение типов**: автоматически обрабатывает разные типы файлов и источники
- **Двусторонняя привязка**: реактивные обновления как для новых файлов, так и для существующих предпросмотров

### **Три гибких режима**

- **Режим Drop**: классический dropzone для выбора новых файлов
- **Режим Preview**: отображение существующих файлов с опциональным взаимодействием (можно расширить через `allowSelectOnPreview`)
- **Режим Edit**: объединённый режим — показывает существующие файлы И позволяет полноценное управление (добавлять, удалять, заменять)

### **Управление файлами**

- **Поддержка Drag & Drop**: нативный HTML5 drag and drop с визуальной обратной связью
- **Валидация файлов**: ограничения по размеру, типам и кастомная валидация
- **Отслеживание прогресса**: встроенный прогресс загрузки с поддержкой серверной загрузки
- **Обработка ошибок**: подробные состояния ошибок и обратная связь пользователю

### **Предпросмотр non-image (иконки)**

Для non-image файлов превью показывает иконку типа файла и имя файла. Тип определяется по MIME и/или расширению.

| Тип | Примеры расширений / MIME |
|---|---|
| `pdf` | `.pdf`, `application/pdf` |
| `xlsx` | `.xls`, `.xlsx`, `application/vnd.ms-excel`, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` |
| `csv` | `.csv`, `text/csv` |
| `audio` | `audio/*`, `.mp3`, `.wav`, `.ogg`, `.flac`, `.m4a`, `.aac` |
| `video` | `video/*`, `.mp4`, `.mkv`, `.webm`, `.mov`, `.avi`, `.mpeg`, `.mpg`, `.m4v` |
| `doc` | `.doc`, `.docx`, `application/msword`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document` |
| `ppt` | `.ppt`, `.pptx`, `application/vnd.ms-powerpoint`, `application/vnd.openxmlformats-officedocument.presentationml.presentation` |
| `archive` | `.zip`, `.tar`, `.gz`, `.tgz`, `.rar`, `.7z` |
| `default` | всё остальное |

### **Варианты раскладки**

- **Позиционирование предпросмотра**: внутри dropzone или снаружи
- **Адаптивный дизайн**: подстраивается под разные размеры экрана и контейнеров
- **Настраиваемые стили**: CSS-переменные для удобной темы
- **Поддержка слотов**: переопределение любой части интерфейса

## 📋 Справочник props

### **Базовая конфигурация**

| Prop         | Type      | Default  | Description                                                                                                        |
|--------------|-----------|----------|--------------------------------------------------------------------------------------------------------------------|
| `modelValue` | `Array`   | `[]`     | Массив объектов `File`, представляющий новые выбранные файлы. Двусторонняя привязка через `v-model`.              |
| `previews`   | `Array`   | `[]`     | Массив строк URL для уже существующих файлов/изображений. Двусторонняя привязка через `v-model:previews`.         |
| `mode`       | `String`  | `"drop"` | Режим поведения компонента: `"drop"` (только новые файлы), `"preview"` (только отображение, можно расширить через `allowSelectOnPreview`), `"edit"` (полная функциональность управления файлами). В режиме `edit` автоматически включается выбор/удаление файлов и появляются особые поведения вроде очистки `previews` при стратегии replace. |
| `multiple`   | `Boolean` | `false`  | Разрешить выбор нескольких файлов.                                                                                |
| `disabled`   | `Boolean` | `false`  | Полностью отключить взаимодействие с dropzone.                                                                     |

### **Выбор файлов и стратегия**

| Prop                     | Type      | Default     | Description                                                                                                                                                                                                                                             |
|--------------------------|-----------|-------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `selectFileStrategy`     | `String`  | `"replace"` | Как новые файлы взаимодействуют с уже существующими: `"replace"` (очистить существующие), `"merge"` (добавить к существующим).                                                                                                                     |
| `allowSelectOnPreview`   | `Boolean` | `false`     | Разрешить выбор и удаление файлов в режиме предпросмотра. Если `true`, клик по пустому месту в контейнере предпросмотра открывает выбор файла, а клики по изображениям/элементам предпросмотра блокируются, чтобы избежать случайного открытия диалога. При `false` режим `preview` является только для чтения. Примечание: для полного редактирования используйте `mode="edit"`. |
| `ignoreOriginalPreviews` | `Boolean` | `false`     | Если `true` и используется стратегия `replace` в режиме `preview`, исходные URL предпросмотра скрываются навсегда после выбора новых файлов, даже если все новые файлы удалить. Если `false`, исходные предпросмотры появятся снова, когда новые файлы удалены. |
| `maxFiles`               | `Number`  | `5`         | Максимальное количество файлов, которое можно выбрать за один раз.                                                                                                                                                                                     |
| `maxFileSize`            | `Number`  | `5`         | Максимальный размер файла в мегабайтах (MB).                                                                                                                                                                                                           |
| `accept`                 | `String`  | `undefined` | Список разрешённых типов файлов (MIME) через запятую. Пример: `"image/*,application/pdf"`.                                                                                                                                                            |

### **Визуальная раскладка и позиционирование**

| Prop                    | Type            | Default    | Description                                                                                 |
|-------------------------|-----------------|------------|---------------------------------------------------------------------------------------------|
| `width`                 | `Number/String` | `auto`     | Ширина контейнера dropzone. Может быть px, %, или любая CSS-единица.                         |
| `height`                | `Number/String` | `200px`    | Высота контейнера dropzone. Может быть px, %, или любая CSS-единица.                         |
| `previewPosition`       | `String`        | `"inside"` | Где показывать предпросмотр: `"inside"` (внутри dropzone), `"outside"` (ниже dropzone).   |
| `imgWidth`              | `Number/String` | `auto`     | Ширина отдельного элемента предпросмотра.                                                   |
| `imgHeight`             | `Number/String` | `auto`     | Высота отдельного элемента предпросмотра.                                                   |
| `previewWrapperClasses` | `String`        | `""`       | Дополнительные CSS-классы для контейнера предпросмотра.                                     |

### **Элементы интерфейса**

| Prop               | Type      | Default           | Description                                                                                                  |
|--------------------|-----------|-------------------|--------------------------------------------------------------------------------------------------------------|
| `showSelectButton` | `Boolean` | `true`            | Показывать кнопку выбора файла внутри dropzone.                                                              |
| `fileInputId`      | `String`  | `auto-generated`  | Пользовательский `id` для скрытого `<input type="file">`. Генерируется автоматически, если не задан.       |
| `state`            | `String`  | `"indeterminate"` | Визуальное состояние: `"indeterminate"`, `"success"`, `"error"` (влияет на цвет рамки).                 |

### **Интеграция с сервером**

| Prop             | Type      | Default     | Description                                                          |
|------------------|-----------|-------------|----------------------------------------------------------------------|
| `serverSide`     | `Boolean` | `false`     | Включить серверную загрузку с отслеживанием прогресса.               |
| `uploadEndpoint` | `String`  | `undefined` | URL эндпоинта для загрузки файлов (когда `serverSide` включён).      |
| `deleteEndpoint` | `String`  | `undefined` | URL эндпоинта для удаления файлов (когда `serverSide` включён).      |
| `serverFileIdKey` | `String` | `undefined` | Ключ/путь в ответе загрузки для замены `fileItem.id` (например `file_id`, `data.file_id`). |
| `headers`        | `Object`  | `{}`        | Дополнительные HTTP-заголовки для запросов к серверу.                |

## 🔄 Справочник событий

| Event               | Payload  | Description                                                                 |
|---------------------|----------|-----------------------------------------------------------------------------|
| `update:modelValue` | `Array`  | Эмитится при изменении списка файлов. Содержит объекты `File`.              |
| `update:previews`   | `Array`  | Эмитится при изменении списка предпросмотров. Содержит строки URL.          |
| `drop`              | `Event`  | Эмитится при drop файлов на dropzone.                                       |
| `fileUploaded`      | `Object` | Эмитится при успешной загрузке файла (серверный режим).                     |
| `fileRemoved`       | `Object` | Эмитится при удалении `File` объекта из списка.                             |
| `previewRemoved`    | `Object` | Эмитится при удалении URL предпросмотра из списка.                          |
| `error`             | `Object` | Эмитится при ошибках валидации или загрузки. Содержит тип ошибки и файлы.   |

## 🎨 Стилизация и кастомизация

### **CSS-переменные**

Компонент использует CSS custom properties для удобной темы:

| Variable                         | Default         | Purpose                               |
|----------------------------------|-----------------|---------------------------------------|
| `--v3-dropzone--primary`         | `94, 112, 210`  | Основной цвет (RGB)                   |
| `--v3-dropzone--border`          | `214, 216, 220` | Цвет рамки (RGB)                      |
| `--v3-dropzone--description`     | `190, 191, 195` | Цвет текста описания (RGB)            |
| `--v3-dropzone--error`           | `255, 76, 81`   | Цвет состояния ошибки (RGB)           |
| `--v3-dropzone--success`         | `36, 179, 100`  | Цвет состояния успеха (RGB)           |
| `--v3-dropzone--overlay`         | `40, 44, 53`    | Цвет подложки оверлея (RGB)           |
| `--v3-dropzone--overlay-opacity` | `0.3`           | Прозрачность оверлея                  |

### **Поддержка слотов**

Переопределяйте дефолтный контент:

| Slot              | Purpose                                              |
|-------------------|------------------------------------------------------|
| `placeholder-img` | Заменить стандартную картинку-заглушку               |
| `title`           | Заменить стандартный заголовок "Перетащите файлы сюда" |
| `button`          | Заменить стандартную кнопку "Выбрать файл"           |
| `description`     | Заменить стандартное описание требований к файлам     |
| `preview`         | Кастомный рендеринг элемента предпросмотра            |
| `remove-button`   | Заменить кнопку удаления в элементе предпросмотра     |
| `progress`        | Заменить прогресс-бар в элементе предпросмотра        |

#### Slot props

| Slot | Props |
|---|---|
| `preview` | `{ data, formatSize, removeFile }` |
| `remove-button` | `{ data, removeFile }` |
| `progress` | `{ data, progress }` |

#### Пример кастомной кнопки удаления

```vue
<template>
  <Vue3Dropzone v-model="files" mode="edit">
    <template #remove-button="{ data, removeFile }">
      <button type="button" @click.stop="removeFile(data)">Удалить</button>
    </template>
  </Vue3Dropzone>
</template>
```

## 🔧 Методы компонента

Доступны через `ref` на компонент:

| Method            | Parameters | Description                                        |
|-------------------|------------|----------------------------------------------------|
| `clearFiles()`    | None       | Удалить все новые `File` объекты                   |
| `clearPreviews()` | None       | Удалить все URL предпросмотра (существующие файлы) |
| `clearAll()`      | None       | Удалить и файлы, и предпросмотры                   |
| `clearPreview()`  | None       | Legacy метод — эквивалент `clearAll()`             |

## 🎯 Сценарии использования

### **Управление товарами**

Идеально для e-commerce, где нужно показывать текущие изображения товара и при этом разрешать добавление/удаление/замену.

### **Обновление профиля/аватара**

Отлично для интерфейсов, где показывается текущая аватарка и можно загрузить новую.

### **Управление документами**

Подходит для форм загрузки документов, где пользователь видит уже прикреплённые файлы и добавляет новые.

### **Галереи**

Хорошо для управления фотогалереей: просмотр существующих фото и добавление новых.

### **CMS (системы управления контентом)**

Удобно для интерфейсов, где редакторы управляют медиа-файлами параллельно с текстовым контентом.

### **Админки для управления медиаконтентом**

Подходит для административных интерфейсов: управление пользовательскими загрузками, системными ассетами или общей библиотекой медиа. Хорошо работает для соцсетей, образовательных систем, корпоративных порталов и любых приложений, где требуется централизованное управление медиа.

## 🚀 Возможности

### **Обработка файлов**

- Автоматически определяет тип файла и показывает подходящий предпросмотр
- Поддерживает изображения (с миниатюрами) и другие типы файлов (с иконками)
- Хранит метаданные и статус загрузки

### **Модели взаимодействия**

- Режим предпросмотра только для чтения
- Интерактивный режим предпросмотра для управления файлами
- Режим редактирования для полного управления файлами
- Умная обработка кликов:
    - Клик по пустой области контейнера предпросмотра открывает выбор файла
    - Блокировка кликов по изображениям и элементам предпросмотра, чтобы избежать случайного выбора файла
    - Отдельные кнопки удаления для каждого элемента предпросмотра

### **Адаптивность**

- Автоматически подстраивается под размер контейнера
- Поддерживает фиксированные и гибкие размеры
- Mobile-friendly touch взаимодействия

### **Профилактика ошибок**

- Проверка размера файла до загрузки
- Проверка типа файла относительно `accept`
- Понятные сообщения об ошибках

Компонент предоставляет полноценное решение для интерфейсов управления файлами с гибкостью, позволяющей адаптировать его под разные сценарии и требования дизайна.
